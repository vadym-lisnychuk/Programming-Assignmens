public interface Express
{
   /* Return the number of occurrences of the character 
      e in the String notice. */
   int swarm(String notice);
   
   /* Return the average number of characters in the String 
      vision and volume. */
   double follow(String vision, String volume);
   
   /* Return a String that consists of n copies of the
      String poll concatenated together. */ 
   String cabin(String poll, int n);
   
   // Return an array consisting of the elements 0, 1, ... , n
   int[] tone(int n);
   
   // Return an array of length n with each element equal to k.
   double[] brink(int n, double k);
   
   /* Return an array of length n where each element contains 
      at least one letter and no two elements are the same. */  
   String[] mark(int n);
}